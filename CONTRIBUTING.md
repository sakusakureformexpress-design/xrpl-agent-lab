# 開発に参加する

このリポジトリは**2人以上で触る前提**に切り替えました。
「何が動いているか」ではなく「**なぜそうなっているか**」が読める状態を保ちます。

---

## まず読むもの（この順番）

| # | 読むもの | 何が分かるか | 所要 |
|---|---|---|---|
| 1 | [`README.md`](README.md) | 何をするツールか・使い方 | 10分 |
| 2 | [`docs/adr/`](docs/adr/) | **なぜその設計なのか。却下した案も含めて** | 20分 |
| 3 | [`docs/91-security-review.md`](docs/91-security-review.md) | 想定している攻撃と、その対処 | 10分 |
| 4 | [`research/`](research/) | 判断の根拠になった調査（出典URL付き） | 必要な時に |
| 5 | [`docs/logs/`](docs/logs/) | 日ごとの作業記録。失敗と訂正も残っている | 必要な時に |

**`docs/adr/` から読むのが一番速いです。**
コードを読めば「どうなっているか」は分かりますが、
「**なぜ他の方法にしなかったか**」はそこにしか書いてありません。

---

## このリポジトリについて（先に言っておきます）

設計の検討と実装は、AI エージェントとの協働で進めています。
**人間の貢献者が従うのは、この CONTRIBUTING.md です。**

そのうえで、以下は本当です:

- **testnet の実機で動いています。** tx ハッシュは `docs/logs/` にあり、
  テストネットのエクスプローラで誰でも検証できます
- **テストは 129 件あり、全部通ります。** ネットワークに出ないので数秒で終わります
- **仕様の誤解で設計を2回直しています**（XLS-80 の効果範囲、`Amount` の可変性。どちらも ADR [0002](docs/adr/0002-支払先の固定方法.md)）。
  **競合についての主張も直しています**（ADR [0006](docs/adr/0006-OWSとは競合しない.md)）。間違いの内容ごと残してあります

**おかしいと思ったところは、遠慮なく言ってください。** レビューを歓迎します。

### HTML のドキュメントについて

`demo/*.html` は **GitHub 上では生ソースに見えます。**
clone して手元のブラウザで開いてください。

| ファイル | 中身 |
|---|---|
| `demo/explainer.html` | 仕組みの説明。**最初に開くならこれ。** 攻撃を受けたときの挙動を触れる |
| `demo/replay.html` | testnet の実行記録を字幕付きで再生 |

---

## 動かす

```bash
npm install
npm test          # 129件。ネットワークに出ないので数秒で終わる
```

testnet の実機確認は別（`src/verify/`）。こちらはネットワークに出ます。

```bash
node src/verify/01-channel-cap.js     # 上限超過が台帳に拒否されることの確認
```

---

## 絶対ルール

守れないと差し戻します。理由も併記します。

### 1. 鍵・シードをコミットしない

`.env` / `*.seed` / `*.key` / `*.policy.json` / `.leash-state.json` は
gitignore 済み。**テストネット以外の鍵はこのリポジトリで扱いません。**

疑わしいときは push 前に:

```bash
git diff --cached | grep -iE "s[A-Za-z0-9]{28}|ED[0-9A-F]{64}|PRIVATE KEY"
```

### 2. 一次ソースを付ける

調査結果・外部仕様の主張には**必ず出典 URL**を付けます。
「〜と言われている」は不可。

理由: このプロジェクトは**過去に2回、仕様の誤解で設計をやり直しています**
（ADR [0002](docs/adr/0002-支払先の固定方法.md) 参照）。
出典があれば、後から検証できます。

### 3. 推測と事実を書き分ける

未確認のものには `[要確認]` を付けます。

### 4. `src/broker/policy.js` に副作用を入れない

**純粋関数のみ。** LLM・ネットワーク・ファイル I/O を入れないでください。
理由は ADR [0003](docs/adr/0003-ブローカーにLLMを入れない.md)。
ここが説得可能になると、このツールの意味が消えます。

### 5. 台帳由来の文字列を `innerHTML` に入れない

アドレス・Memo・結果コードは**外部が書ける領域**です。
表示は `textContent` 限定。ADR [0007](docs/adr/0007-ダッシュボードに鍵を持たせない.md)。

---

## 変更の出し方

### ブランチを切る

```bash
git switch -c feat/短い説明
```

### コミットメッセージ

**1行目は「何をしたか」、本文は「なぜか」。**

```
feat(broker): 支払先のドメインを双方向に検証する

402 応答の resource フィールドは攻撃者が書ける。
これを信用すると、任意のドメインを名乗って支払いを引き出せる。

実際に叩いた URL のホスト名を呼び出し側から渡し、
そのドメインの xrp-ledger.toml が当該アドレスを claim しているかを確認する。
片方向だと、攻撃者が自分のドメインに被害者のアドレスを書くだけで通る。
```

日本語で構いません。**「なぜ」が無いコミットは、半年後の自分にも読めません。**

### Pull Request を開く

`main` へ直接 push せず、PR を開いてください。
`.github/pull_request_template.md` が自動で入ります。埋めてください。

PR の本文は**レビューする人が最初に読むもの**です。
「何を変えたか」より「**なぜその判断にしたか**」と「**どこを見てほしいか**」を書いてください。

### 設計を変えたなら ADR を足す

以下のどれかに当てはまるなら、`docs/adr/` に1本足してください:

- 他の方法もあったが、どれかを選んだ
- 前の判断を覆した
- **やらないことを決めた**

書式は [`docs/adr/README.md`](docs/adr/README.md)。5節だけです。
**「却下した案」が本体**で、他は埋めるだけです。

---

## レビューの前提

**書いた人が自分でレビューしても意味がない**、が原則です。
AI エージェントで書いたものも、別のセッションに読ませてから取り込んでいます。

人間同士でも同じです。相手の PR は、動くかどうかではなく
**「乗っ取られたときにどうなるか」**で見てください。それがこのツールの主題です。

---

## 開いている問い

答えが出ていないものです。意見があれば Issue か PR でお願いします。

| 問い | 状況 |
|---|---|
| **ドル建て（RLUSD）での分割払い出し** | Payment Channel は XRP 限定。Escrow は全額か0か。「支払いごとに1本張る」か「XRP建てで運用しドル換算表示」か、まだ決めていない |
| **OWS のポリシーバックエンド化** | ブローカーを OWS の policy 形式に寄せたい。OWS がチャネルのクレーム署名のような「台帳外の署名」をどう扱うかが `[要確認]` |
| **メインネット対応** | 鍵の取り扱いを固める必要がある。設計から相談したい |
| **npm 公開の時期** | `package.json` の整備（`files` の限定・入口ファイル）とメインネット対応の設計が先。0.1.0 から出す想定 |
| **XLS-75（Permission Delegation）をどう扱うか** | 支持が維持されれば最速 2026-10-05 に有効化（メインネットの `feature` で確認）。上限には使えないと確認済みだが、補充の委譲・`SettleDelay` の妥当性・granular permission の標準提案について意見がほしい → [`research/2026-09-21-xls75-permission-delegation.md`](research/2026-09-21-xls75-permission-delegation.md) |
