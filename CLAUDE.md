# CLAUDE.md — xrpl-agent-lab

## このプロジェクトの目的

**XRPL Grants（AI Fund / Japan-Korea Fund）への採択**を唯一のゴールとする。
ハッカソン参加は対象外（NY現地開催のため不参加を決定済み）。

## 言語

- ユーザーとのやりとり、`docs/` のドキュメントは **日本語**
- コード内の識別子は英語、コメントは日本語で可
- **申請書・README・デモ動画は英語**（審査員は英語話者）

## 絶対ルール

1. **一次ソース原則。** 調査結果には必ず出典URLを付ける。URLのない主張は
   Critic が無条件で差し戻す。「〜と言われている」は禁止。
2. **Builder と Critic は同一セッションで兼任しない。** 自己レビューは無効。
3. **受入基準を書く前に実装しない。** 何を満たせば完了かを先に `docs/20-backlog.md`
   に書く。
4. **秘密鍵・シードを絶対にコミットしない。** `.env` は gitignore 済み。
   テストネット以外の鍵はこのリポジトリで扱わない。
5. **推測と事実を書き分ける。** 未確認事項は `[要確認]` を明示する。

## ディレクトリ

| パス | 用途 |
|---|---|
| `docs/00-strategy.md` | 戦略・仮説・意思決定の記録 |
| `docs/10-rubric.md` | Judge の採点表（審査基準） |
| `docs/20-backlog.md` | バックログと受入基準 |
| `docs/30-grant-draft.md` | 申請書ドラフト（育て続ける） |
| `docs/logs/` | スプリント記録（1日1ファイル） |
| `research/` | Scout の調査結果（出典URL必須） |
| `src/` | 実装 |
| `demo/` | デモ台本・録画 |

## スプリントの回し方

1日1スプリント。`/sprint` 相当の流れを PM が統括する。

```
[P] PM      → 目標と受入基準を 20-backlog.md に記述
[D] Scout / Architect / Builder が実行
[C] Critic  → 受入基準チェック
    Judge   → rubric 採点（70点未満は差し戻し）
[A] PM      → バックログ更新、logs/ に記録
```
