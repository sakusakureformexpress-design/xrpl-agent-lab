# CLAUDE.md — XRPL Leash

AI エージェントの支出上限と支払先を XRP Ledger に強制させるツールキット。

## このリポジトリは公開されている

**置いてよいのは、公開して問題ないものだけ。**

- コード、テスト、設計判断（`docs/adr/`）、技術調査（`research/`）、仕様の提案、セキュリティレビュー、作業ログ
- **置かないもの:** 個人情報、実在の人物名、プロジェクト運営のための内部文書、他のプロジェクトを名指しで評価する文書
  （これらはリポジトリの外で管理する）

## 言語

- ドキュメントとコメントは日本語。コードの識別子は英語
- `README.en.md` は英語。README を変えたら両方を揃える
- 紹介用のスライド等はこのリポジトリに置かない

## 絶対ルール

1. **秘密鍵・シードをコミットしない。** `.env` は gitignore 済み。テストネット以外の鍵は扱わない
2. **一次ソース原則。** 外部仕様についての主張には出典 URL を付ける。報道より仕様書を優先する
3. **推測と事実を書き分ける。** 未確認のものは `[要確認]`
4. **`src/broker/policy.js` は純粋関数のみ。** LLM・ネットワーク・ファイル I/O を入れない（ADR 0003）
5. **台帳由来の文字列は `textContent` で描画する。** `innerHTML` に入れない（ADR 0007）
6. **書いたものは、別のセッションにレビューさせてから取り込む。** 自己レビューは無効

## ディレクトリ

| パス | 用途 |
|---|---|
| `src/` | 実装 |
| `test/` | テスト（`npm test`） |
| `docs/adr/` | 設計判断の記録 |
| `docs/logs/` | 作業ログ（testnet の tx ハッシュ） |
| `research/` | 技術調査（出典 URL 必須） |
| `demo/` | 説明ページとデモ |
