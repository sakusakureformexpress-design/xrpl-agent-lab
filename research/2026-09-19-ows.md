# Open Wallet Standard (OWS) の調査 — 競合か、土台か

調査日: 2026-09-19
きっかけ: CoinDesk 2026-09-17 の記事
「Ripple adds XRP payments to Stripe and Tempo's AI standard in new developer kit」

## 結論

**OWS は、我々の第2段（ブローカー）とほぼ同じことをする。しかも公式に採用済み。**
**だが第1段（Payment Channel）に相当するものは無い。**

したがって**競合として戦うのではなく、OWS の上に乗る**のが正しい。

## 発端となった記述

> The Open Wallet Standard integration "lets agents request transactions without
> accessing private keys and **enforces safeguards such as spending limits and
> approved destinations**."
> — https://www.coindesk.com/tech/2026/09/17/ripple-adds-xrp-payments-to-stripe-and-tempo-s-ai-standard-in-new-developer-kit

**「支出上限」と「承認済みの支払先」。** 言葉の上では我々と同じ。

## OWS とは何か

| 項目 | 内容 | 出典 |
|---|---|---|
| 主導 | MoonPay。2026-03-23 公開。MIT | https://www.moonpay.com/newsroom/open-wallet-standard |
| 貢献者 | **20以上の組織。Ripple 自身も含む。** PayPal / OKX / **Ripple** / Tron / TON Foundation / Solana Foundation / Ethereum Foundation / Base / Polygon / Sui / Filecoin Foundation / LayerZero / Arbitrum / Circle ほか | 同上 |
| 対応チェーン | **XRPL を含む10のチェーンファミリ** | Ripple v1.1 発表 |
| 鍵の保管 | `~/.ows/wallets/` に AES-256-GCM で暗号化 | https://openwallet.sh/ |
| アクセス | CLI / MCP サーバ / SDK / REST API | https://docs.openwallet.sh/ |
| XRPL 採用 | **Starter Kit v1.1（2026-09-15）で公式に統合** | Ripple |

### ポリシーエンジンが強制するもの

MoonPay のニュースルーム（一次ソース）:

> "**Spending limits, contract allowlists, chain restrictions, and time-bound
> authorizations** are enforced at the wallet layer."

GitHub の README（一次ソース。より狭い）:

> "A pre-signing policy engine gates agent (API key) operations before decryption —
> **chain allowlists, expiry, and optional custom executables.**"

**支出上限とチェーン制限は明確に重なる。** ただし下記の通り、支払先の制限は同じではない。

### [要確認] 「承認済みの支払先」は OWS 自身の言葉ではない

"approved destinations" は **CoinDesk の記者の表現**であり、OWS 側の一次ソースにこの語は無い。
OWS 自身が書いているのは "**contract** allowlists"（＝EVM のコントラクトアドレス）である。

**XRPL の通常送金に対して、任意の支払先アドレスの許可リストを OWS が強制できるかは未確認。**
できないなら、我々のブローカーの支払先束縛（`xrp-ledger.toml` × `Domain` の双方向検証）は
**重複ではなく差分**になる。ここは実装を読んで確かめる価値がある。

### [要確認] ポリシーの適用範囲

README は "gates **agent (API key) operations**" と書いている。
アーキテクチャ図の注記も "Policy engine evaluates **for API tokens**"。
**ローカルの SDK / CLI から直接呼ぶ場合にもポリシーが効くのかは、この記述からは読み取れない。**

## 決定的な違い: 強制の場所

### OWS

> 政策評価は**署名の前に**行われる。"Keys never cross this boundary"。
> 署名時は「鍵を復号 → 署名 → 即座に消去」。

**vault が秘密鍵を保持し、ポリシーに反する要求には署名を拒否する。**
つまり **ソフトウェアによる強制**である。

鍵の扱い（MoonPay ニュースルーム）:

> keys are "encrypted at rest using **AES-256-GCM**, decrypted only to produce a
> signature, held in **protected memory that cannot be swapped to disk**, and
> **wiped immediately after**."

**丁寧な設計である。エージェントが乗っ取られても止まる。**

### 訂正: 「脅威モデルの記載が無い」は言い過ぎだった

当初そう書いたが、**根拠が不十分だった。**
`docs.openwallet.sh` には `05 — Key Isolation` という節があり、enclave にも言及がある。
ただしその本文は JavaScript で読み込まれるため WebFetch では取得できず、
**中身を確認できていない。** よって「記載が無い」とは書けない。

**代わりに、OWS 自身の図から確実に言えることを使う。**
README のアーキテクチャ図は、Policy Engine・Signing Core・Wallet Vault の3つを
**同一プロセス・同一マシン上**に描いており、Signing Core には明示的に
**`(in-process)`** と注記されている。さらに鍵の説明は
"**Current implementations** harden in-process memory handling" と書かれている
（＝現状は in-process であり、それが前提）。

つまり**ポリシーも鍵も、同じ機械の上にある。**
その機械を掌握した者は、両方に手が届く。これは実装の穴ではなく、
**この置き方の構造的な帰結**である。

### 我々（第1段: Payment Channel）

台帳が拒否する。エージェントが持つのはチャネルの署名鍵だけで、
**口座の鍵はどこにも渡さない。**
クライアント側が完全に掌握されても、上限は台帳側にあるため動かない。

### 並べると

| | OWS | XRPL Leash 第1段 |
|---|---|---|
| 上限を強制するのは | **vault（ソフトウェア）** | **台帳** |
| 秘密鍵の所在 | vault が保持 | 人間の手元のみ |
| エージェントが持つもの | 署名の要求権 | チャネルの署名鍵のみ |
| 署名マシンが侵害されたら | **鍵がそこにある** | **上限は動かない** |
| ポリシーと鍵の所在 | **同一マシン・同一プロセス** | ポリシーは台帳、鍵は人間の手元 |

## 我々の記述で訂正が必要なもの

- ❌ 「エージェントの支出上限を強制する OSS が存在しない」
  → **誤り。OWS が存在する。** ただし**オンチェーンで強制するもの**は依然として無い
- ピッチデックの統計「0 — open-source tools that enforce an agent's ceiling
  **on-chain**」は、"on-chain" の限定があるため**事実として維持できる**。
  ただし OWS に言及せずに使うのは不誠実なので、明示的に触れる

## 戦略の変更

### 変えること

**第2段（ブローカーのポリシーエンジン）で競わない。**
MoonPay が主導し、**Ripple を含む20以上の組織**（PayPal・Circle・OKX・
Ethereum Foundation・Solana Foundation ほか）が貢献している標準と、
個人が同じものを作って勝つ理由が無い。

**Ripple 自身が貢献者である**という事実は、当初の記録から漏れていた。
「Ripple が採用した」より強い。**審査側にとって OWS は自分たちの標準である。**

### 残ること

**第1段（Payment Channel による台帳強制）は OWS に存在しない。**
OWS の上限は vault というソフトウェアが持ち、そのソフトウェアは鍵と同じ機械の上にある。
**機械ごと取られたときに残るものが、こちらには有る。**

### 新しい位置づけ

> OWS は上限を vault に置く。侵害されたエージェントは止められる。
> **だが侵害されたホストは止められない** — Policy Engine も Signing Core も Wallet Vault も
> 同じ機械の上にあると、OWS 自身の図が示している。
> XRPL Leash は同じ上限を台帳に置く。エージェントもホストも動かせない。
> **そして OWS を置き換えるのではなく、その上に乗る。**

この形なら「Open Standards, Not an Island」という Ripple 自身の方針とも一致する。

## ロードマップへの反映

- [ ] **ブローカーを OWS のポリシーバックエンドとして実装し直す**検討。
      独自のポリシー JSON をやめ、OWS の policy に寄せる
- [ ] OWS の vault から XRPL の鍵を扱いつつ、上限は Payment Channel に置く構成の検証
- [ ] [要確認] OWS のポリシーエンジンが、チャネルのクレーム署名のような
      「台帳外の署名」をどう扱うか

## 正直な評価

**これは我々にとって、むしろ好材料である。**

1. 「誰もやっていない」という弱い主張から、
   「**標準が届かない層を埋める**」という具体的な主張に変わった
2. 我々は自分のブローカーについて
   **「機械を掌握されたら残高まで失われる」と既に明記していた**。
   同じ限界が OWS にもあることを示せる立場にある
3. 競合ではなく補完として提案できるため、採択後の統合パスも描ける

**リスク**: 審査員に「OWS でいいのでは」と言われること。
→ ピッチで**先回りして正面から扱う**。曖昧にすると致命傷になる。


---

## 出典（2026-09-19 再確認）

| 主張 | 出典 | 種別 |
|---|---|---|
| "enforces safeguards such as spending limits and approved destinations" | https://www.coindesk.com/tech/2026/09/17/ripple-adds-xrp-payments-to-stripe-and-tempo-s-ai-standard-in-new-developer-kit | 報道 |
| ポリシー項目・AES-256-GCM・MIT・2026-03-23・貢献者一覧 | https://www.moonpay.com/newsroom/open-wallet-standard | 一次 |
| `~/.ows/wallets/`・in-process 署名・アーキテクチャ図・"chain allowlists, expiry, and optional custom executables" | https://github.com/open-wallet-standard/core （README） | **一次（実装）** |
| "policy-gated `ows_sign` interface"・vault `~/.ows/`・"decrypted only long enough to sign, then wiped from memory" | https://dev.to/ripplexdev/the-xrp-ledger-ai-starter-kit-v11-open-standards-not-an-island-358k | **一次（Ripple 自身）** |
| Payment Channel の位置づけ（"vouchers can be signed at rates exceeding 100,000 per second"） | 同上 | 一次 |
| ポリシーエンジンの詳細仕様 | https://docs.openwallet.sh/doc.html?slug=03-policy-engine | **未取得**（JS 読み込みのため） |
| 鍵分離・脅威モデル | https://docs.openwallet.sh/doc.html?slug=05-key-isolation | **未取得**（同上） |

**最後の2つは読めていない。** 「OWS はこう書いていない」という形の主張には使わないこと。
